<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gobinda.loginregistration"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15"   ></uses-sdk>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_smart_aptitude_test"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.Register"
            ></activity>
        <activity android:name=".view.Exam"></activity>
        <activity
            android:name=".view.Rules"
            ></activity>
        <activity 
            android:name=".view.Score"></activity>
    </application>
    

</manifest>